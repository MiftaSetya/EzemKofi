<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EzemKofi" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/EzemGreen</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EzemKofi" parent="Base.Theme.EzemKofi" />
</resources>